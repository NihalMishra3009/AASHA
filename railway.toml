[build]
builder = "railpack"
buildCommand = "cd backend && bash mvnw clean package -DskipTests"

[deploy]
startCommand = "cd backend && java -jar target/asha-backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/healthz"
